<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.brett.calftracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCalfActivity" />
        <activity android:name=".CalfListActivity" />
        <activity android:name=".CalfProfileActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".CreateFarmActivity" />
        <!-- <activity -->
        <!-- android:name=".menu" -->
        <!-- android:label="@string/title_activity_menu" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".NewCalfVaccineSelectionActivity" />
        <activity android:name=".CalfProfileMedicalHistoryActivity" />
        android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ProtocolActivity" />
        <activity android:name=".EditMedicineActivity" />
        <activity android:name=".EditVaccineActivity" />
        <activity android:name=".MedicineActivity" />
        <activity android:name=".IllnessActivity"></activity>
    </application>

</manifest>